NIN Python
===========

This repository implements the Phase-1 transformation layer for the NIN rebuild. It reads raw SAP export files, standardizes and transforms them, and writes a deterministically reproducible base table consumed by the existing Excel workbook.

See NIN_Python_Plan.md for full project plan.
